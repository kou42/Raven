# Raven 開発指示

このファイルは、CodexなどのコーディングエージェントがRavenリポジトリを編集するときに従うプロジェクト共通ルールです。
実装速度よりも、既存設計の把握、変更理由の明確化、安全な差分、学習しやすい説明を優先してください。

## 1. 作業開始時の原則

- コード内容・構造・依存関係を把握する必要がある場合は、**最初にリポジトリルートの `structure.txt` を確認する**。
- `structure.txt` で関連モジュールの位置を把握してから、関連するヘッダ・実装・設定ファイルを読む。
- 既存の仕組みと同じ責務を持つ型やSystemがないか検索し、既存設計を確認せずに新しい仕組みを追加しない。
- 変更対象だけでなく、その呼び出し元・呼び出し先・Lifetime・所有権・データの流れも必要に応じて確認する。
- 大きな変更では、実装前に「現在の構造」「問題点」「変更方針」を簡潔に整理する。
- **`Root/Root/Raven` 以下のディレクトリ構成またはファイル構成に変更が入った場合は、その変更内容に合わせてリポジトリルートの `structure.txt` も同じ作業内で必ず更新する。**
- ファイルやディレクトリの追加・削除・移動・名称変更を行った場合は、作業完了前に `structure.txt` と実際の `Root/Root/Raven` 以下の構成が一致していることを確認する。
- **Visual Studioプロジェクトで管理する新しいファイルを作成した場合は、対応する `.vcxproj.filters` も同じ作業内で必ず更新し、既存の論理フォルダー構成に合わせて適切なFilterへ登録する。**

## 2. 実装方針

- 必要最小限の変更で目的を達成する。
- 無関係なリファクタリングや整形を同じ変更へ混ぜない。
- 既存APIを変更する場合は、利用箇所を検索して影響範囲を確認する。
- 暫定実装と正式なEngine機能を区別し、Asset固有・Demo固有・Debug固有の知識を汎用Runtimeへ不用意に持ち込まない。
- Demo Layerは検証の入口として扱い、再利用可能な設定・Runtime処理・Asset固有設定は適切なモジュールへ分離する。
- Gameplay設定、Asset初期設定、Runtime調整値は役割が異なる場合、暗黙に同一視せず明示的に分離する。
- Lifetimeや非所有参照を扱う場合は、生成順・破棄順・参照先の生存期間を必ず確認する。
- 座標系、Transform積順、単位、正規化基準など、ゲームエンジンで不具合になりやすい前提を明示する。

## 3. C++ コーディング規約

- `if` 文内の処理は、1行であっても必ず `{}` で囲む。
- `!condition` はできるだけ使用せず、`condition == false` のように明示的に比較する。
- nullチェックは `if (ptr == nullptr)` / `if (ptr != nullptr)` のように明示する。
- bool値も意図が読みやすい場合は `== true` / `== false` を使用する。
- 既存コードの命名規則、namespace、型設計、include方針を尊重する。
- 不要な動的メモリ確保や所有権の曖昧なraw pointerを増やさない。
- Magic Numberを追加する場合は、意味のある設定値・定数・Profileへ移せないか検討する。

## 4. コメント方針

- **既存コメントは原則として削除しない**。
- 実装変更によってコメントが事実と異なる場合は、コメントも新しい実装に合わせて更新する。
- 重要な処理ほど日本語コメントを丁寧に記載する。
- コメントでは「何をしているか」だけでなく、可能な限り「なぜ必要なのか」「何を防いでいるのか」も説明する。
- 特に以下は詳しくコメントする。
  - Lifetime / Ownership
  - Physics / Collisionの前提
  - Animationの時間・速度・Blend条件
  - Transform / 座標系 / 行列積順
  - Asset固有設定とRuntime設定の境界
  - fallbackやエラー時に処理を継続する理由
  - Performance最適化で省略している処理やFast Pathの成立条件
- コードから自明な内容を日本語へ言い換えるだけのコメントは増やしすぎない。

## 5. 実装時の推奨手順

1. `structure.txt` を確認する。
2. 関連コードを検索して読む。
3. 呼び出し関係・データフロー・所有関係を把握する。
4. 現状の問題と変更方針を整理する。
5. 必要最小限の差分で実装する。
6. `Root/Root/Raven` 以下の構成を変更した場合は、`structure.txt` を実際の構成に合わせて更新する。
7. `git diff` 相当の差分を確認し、意図しない削除・整形・コメント消失がないか確認する。
8. 可能であればビルドする。
9. 関連するテストやDemoが存在する場合は実行する。
10. エラーやWarningが発生した場合は原因を調査し、今回の変更に起因するものを修正する。
11. 最後に変更内容と残課題を整理する。

## 6. ビルド・検証

- 実装完了を宣言する前に、可能な範囲でビルドまたはテストを実行する。
- ビルドできない環境の場合は、ビルド済みであるかのように扱わず、未検証であることを明示する。
- テストが存在しない機能では、確認すべきRuntime挙動を具体的に提示する。
- Debug表示やCounterがある場合は、機能の正しさだけでなく診断値が実際のRuntime状態と一致しているか確認する。
- 修正のために既存Safety Checkやfallbackを安易に削除しない。

### 6.1 ビルド・テスト確認時のトークン節約

- ビルドログ全体を無条件に読み込んだり、チャットへ大量に出力したりしない。
- 成功時は、ビルド成功／失敗、終了コード、Warningの有無、テスト成功／失敗件数など必要最小限の情報だけを確認・報告する。
- ビルド失敗時も最初からログ全体を読まず、`error`、`fatal error`、`FAILED`、コンパイラエラーコード、リンカエラー、ファイル名、行番号を優先して抽出する。
- 原因特定に必要な場合のみ、該当エラー周辺のログを追加で確認する。
- Warningが大量にある場合は全文を展開せず、総数、種類、今回の変更によって新しく発生した可能性があるWarningを優先して確認する。
- 既存コード由来で今回の変更と無関係なWarningは、原則として詳細ログを展開しない。
- 可能であれば、変更対象のコンパイル確認 → 関連ターゲットのビルド → 関連テスト → 必要な場合のみ全体ビルド、の順に段階的に確認する。
- 毎回無条件にClean Buildやフルビルドを実行せず、通常のコード変更ではインクリメンタルビルドを優先する。
- Clean Buildは、CMake設定・ビルド構成・ABI・リンク構成・依存関係を変更した場合、インクリメンタルビルドで問題を再現できない場合、または最終確認として必要な場合に限定する。
- コマンド出力は可能であればログファイルへ保存し、まず終了コードを確認する。失敗時のみログからエラー箇所や必要な末尾・周辺行を確認する。
- GitHub Actionsも、最初にWorkflowの成否、失敗Job、失敗Stepを確認し、失敗している場合のみ該当Stepのエラー周辺を読む。
- ビルドログ全文は、ユーザーが明示的に要求した場合を除いてチャットへ表示しない。

## 7. Git運用

- コミットメッセージは日本語で記載する。
- 1コミットには可能な限り1つの論理的変更だけを含める。
- 無関係な変更を同じコミットへ混ぜない。
- コミット前に差分を確認する。
- 大規模変更では、設定構造追加、Runtime対応、利用側移行など、安全に分割できる単位を検討する。
- 既存ブランチが最新の基準ブランチから遅れている場合は、その状態を確認してから大きな変更を重ねる。

### 7.1 GitHubコネクタとチャットモード

- RavenのGitHubリポジトリ操作が必要な場合、「Work modeでしか編集できない」と即断しない。
- まず現在利用可能なGitHubコネクタの機能と `kou42/Raven` に対する権限を確認する。
- GitHubコネクタ経由で可能なリポジトリ情報・権限確認、ファイル読み込み・作成・更新・削除、ブランチ作成、コミット、Pull Request作成、diff確認、追加修正、PRマージ、GitHub Actions結果確認は、原則としてチャットモードのまま実行する。
- 特別な理由がない限り `master` へ直接変更せず、`master` → 作業ブランチ → 実装・コミット → diff確認 → Pull Request → 必要な追加修正 → 最終確認 → `master` へマージ、の流れを優先する。
- ブランチ名は `feature/...`、`fix/...`、`refactor/...` など変更内容が分かる名前にする。
- Pull Requestのタイトル・説明も原則として日本語で記載する。
- GitHubコネクタでは実行できず、ローカルcheckout、CMake configure/build、Visual Studio/MSBuild、コンパイラエラーの再現、ローカルテスト、Raven実行ファイルの起動、GPU/OpenGL/DirectXなど実行環境依存の確認が実際に必要な場合に限りWork modeを使用する。
- Work modeが必要な場合も、最初から全作業を移行せず、GitHubコネクタだけで実行可能な部分とローカル環境が必要な部分を切り分ける。

## 8. 作業完了時の報告

実装完了後は、少なくとも以下を日本語で説明する。

- 何を変更したか。
- なぜその設計にしたか。
- 重要なデータフローや責務の境界。
- ビルド・テストを行ったか。行っていない場合はその旨。
- 残っている暫定実装・技術的負債・注意点。
- 次に進めるおすすめロードマップ。
